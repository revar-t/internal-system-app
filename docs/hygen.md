# Hygen 使い方まとめ

## 目的

- ファイル生成を自動化してフォーマットを統一する
- 再利用可能なテンプレートでコンポーネント作成を効率化

---

## 使用方法

### 1. 新しいコンポーネントの作成

```bash
npx hygen component new
```

- プロンプトが表示されるのでコンポーネント名を入力
- ケバブケースでファイル名を入力
- `_templates/component/new` のテンプレートに基づきファイルが自動生成される
